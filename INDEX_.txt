CREATE INDEX index_employee_name
ON EMPLOYEE (FIRSTNAME, LASTNAME);

CREATE INDEX index_employee_uName
ON EMPLOYEE (USERNAME);

COMMIT;

CREATE INDEX index_loan_val
ON LOAN (LOANVALUE);

CREATE INDEX index_tax_val
ON TAX (TAXVALUE);

CREATE INDEX index_tax_year
ON TAX (TAXYEAR);

COMMIT;